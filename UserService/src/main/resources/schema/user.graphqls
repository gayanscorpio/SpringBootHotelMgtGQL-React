type User {
    id: ID!
    name: String!
    email: String!
}

input UserInput {
    name: String!
    email: String!
}

type Query {
    userById(id: ID!): User
    allUsers: [User]
    usersByIds(ids: [ID!]!): [User!]!
}

type Mutation {
    createUser(user: UserInput!): User
}
